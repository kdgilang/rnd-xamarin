<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Poseidon.Models.Print"
             x:Class="Poseidon.MainPage"
             Shell.PresentationMode="Animated">

    <ContentPage.BindingContext>
        <vm:PrintPageViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <Label Text="Todo ..."></Label>
            <!--<Picker ItemsSource="{Binding Devices}"
                    Title="Select Device"
                    ItemDisplayBinding="{Binding .}"
                    SelectedItem="{Binding SelectedDevice}"></Picker>-->

            <!--<Label Text="{Binding PrintText}" TextColor="Red"></Label>
            <Button Text="Print" Command="{Binding PrintCommand}" />-->
        </StackLayout>
    </ContentPage.Content>

</ContentPage>
